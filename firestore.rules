rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all by default to protect cloud data
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
